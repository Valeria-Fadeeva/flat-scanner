# TODO — Flat Scanner

## ✅ Завершено

- [x] Единая конфигурация (config.toml) для сервера и клиента
- [x] XDG Base Directory Specification для путей
- [x] Резолвинг `~` в `$HOME` в Rust и Dart
- [x] Создание каталогов при установке (PKGBUILD)
- [x] Парсинг TOML в Dart клиенте
- [x] Единая техническая спецификация (TECH_SPEC_UNI.md)

## 🟡 Требует доработки

### 1. Эндпоинты API

**Проблема:** Клиент не использует эндпоинт `POST /api/v1/scan` из `routes.rs`

**Текущее состояние:**
- Клиент использует: `POST /api/v1/scanner/process` (основной)
- Сервер имеет: `POST /api/v1/scan` (из routes.rs, не используется клиентом)

**Решение:**
- [ ] Определить, нужен ли `POST /api/v1/scan` как альтернативный эндпоинт
- [ ] Если не нужен — удалить из routes.rs или пометить как deprecated
- [ ] Если нужен — добавить метод в ApiService клиента

### 2. Предупреждения компилятора

**Проблема:** Есть неиспользуемые импорты и функции

**Список предупреждений:**
- [ ] `safe_calculate_homography` — неиспользуемый импорт (cv/mod.rs:57)
- [ ] `validate_page_geometry` — неиспользуемый импорт (cv/mod.rs:57)
- [ ] `extract_seal_mask` — неиспользуемый импорт (cv/mod.rs:60)
- [ ] `overlay_seal_on_text` — неиспользуемый импорт (cv/mod.rs:60)
- [ ] `height` — неиспользуемая переменная (cv/warping.rs:126)
- [ ] `digitization_error_to_response` — неиспользуемая функция (routes.rs:26)

**Решение:**
- [ ] Удалить неиспользуемые импорты или добавить `#[allow(dead_code)]`
- [ ] Удалить неиспользуемую функцию `digitization_error_to_response` или использовать её

### 3. Тесты

**Проблема:** Один тест падает из-за несовместимости с OpenCV 5.0.0

**Список:**
- [ ] `test_detect_spine_shadow` — падает из-за функции `reduce` с неверным оператором

**Решение:**
- [ ] Исправить вызов `reduce` в `detect_spine_shadow` (использовать REDUCE_AVG вместо REDUCE_SUM)
- [ ] Протестировать с OpenCV 4.x и 5.x

### 4. Детекция типа страницы (обложка vs разворот)

**Проблема:** Алгоритм описан в спецификации, но не реализован

**Список:**
- [ ] Реализовать `detect_page_type()` в `cv/segmentation.rs`
- [ ] Интегрировать в `pipeline.rs`
- [ ] Логика обработки Cover (без сегментации)
- [ ] Сохранение Cover как одной страницы в БД

### 5. Интеграция seal_extraction

**Проблема:** Модуль реализован, но не интегрирован в пайплайн

**Список:**
- [ ] Вызвать `extract_seal_mask` в `pipeline.rs` после бинаризации
- [ ] Вызвать `overlay_seal_on_text` для наложения печатей
- [ ] Добавить тесты для интеграции

## 📋 Низкий приоритет

### 6. Zero-Copy передача превью

**Проблема:** Передача RAW-буфера во Flutter может вызвать фриз UI

**Список:**
- [ ] Преобразовать RAW в JPEG/WebP на сервере
- [ ] Передать указатель на Uint8List через Zero-Copy
- [ ] Протестировать производительность

### 7. Оптимизации

**Список:**
- [ ] Оптимизация `detect_spine_shadow` через OpenCV reduce
- [ ] Пул буферов для Sauvola бинаризации
- [ ] Сжатие изображений в PDF (Flate)

## 📚 Документация

**Список:**
- [ ] Обновить README.md с новой структурой конфигурации
- [ ] Добавить примеры использования config.toml
- [ ] Документировать эндпоинты API (OpenAPI/Swagger)