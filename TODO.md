# 🔥 NEXT LEVEL TODO: СБОРКА И ОТЛАДКА ВЫЧИСЛИТЕЛЬНОГО ЯДРА RUST

## [ ] ЭТАП 1: ФИЗИЧЕСКАЯ КОМПИЛЯЦИЯ В ARCH LINUX
* [x] Убедиться в наличии системных пакетов: `sudo pacman -S opencv clang tesseract`
* [x] Инициализировать репозиторий в папке проекта: `cargo init`
* [x] Залить файлы `Cargo.toml`, `main.rs` и структуру папки `/cv` на диск.
* [ ] Прогнать тестовую сборку ядра: `cargo build --release`
* [ ] Запустить сервер на Axum: `./target/release/kanonissa_core`
* [ ] Проверить отклик порта из терминала: `curl http://127.0.0`

## [ ] ЭТАП 2: ИНТЕГРАЦИЯ С ДРАЙВЕРОМ SANE (ЛИКВИДАЦИЯ ЗАГЛУШЕК)
* [ ] Подключить крейт низкоуровневых биндингов `sane-sys` или реализовать модуль `std::process::Command` для прямого захвата RAW-потока утилиты `scanimage`.
* [ ] Заменить генерацию `dummy_frame` в `main.rs` на чтение реального массива байт (`Vec<u8>`) из памяти сканера Epson.

## [ ] ЭТАП 3: РАЗВЕРТЫВАНИЕ ЭКСПОРТЕРА CCITT GROUP 4
* [ ] Написать логику упаковки бинарного растра Сауволы в PDF-контейнер с применением двумерного кодирования серий T.6.
