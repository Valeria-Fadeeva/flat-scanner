
# TODO: Программный комплекс оцифровки книг «Канонисса-Библиотека» v1.0

## Инструкция по работе с ИИ-агентом (Промпт-директива)
> **СТРОГОЕ ПРАВИЛО ДЛЯ QWEN:** Ты — ведущий системный архитектор. Тебе запрещено использовать плейсхолдеры (`// TODO`, `...`, `/* код здесь */`). Пиши каждую функцию со 100% реализацией. Если лимит токенов заканчивается, прервись на закрытии функции и напиши: `[ОЖИДАНИЕ КОМАНДЫ ПРОДОЛЖАЙ]`.

---

## Этап 1: Проектирование FFI-интерфейса и структуры данных
*Выполняется в первую очередь. Слой Dart и слой Rust должны жестко договориться о типах данных.*

- [ ] **1.1. Описание общих структур (C-Compatible):**
  - [ ] Создать структуру метаданных страницы (ширина, высота, углы, статус обработки).
  - [ ] Создать структуру конфигурации фильтров (пороги Кэнни, параметры бинаризации).
- [ ] **1.2. Разработка заголовков ядра на стороне Rust:**
  - [ ] Написать функции выделения и очистки памяти в куче Rust для буферов изображений (`extern "C" fn alloc_buffer`, `free_buffer`).
  - [ ] Описать сигнатуры функций конвейера (`extern "C" fn detect_quad`, `extern "C" fn process_image`).
- [ ] **1.3. Разработка FFI-моста на стороне Dart:**
  - [ ] Описать аналоги структур через `package:ffi` (`Struct`, `Pointer<Uint8>`).
  - [ ] Написать класс-обертку загрузки динамической библиотеки (`.so` / `.dll`) через `DynamicLibrary.open`.

---

## Этап 2: Вычислительное ядро на Rust (Математический конвейер)
*Пишется атомарно, функция за функцией. На вход принимаются только сырые байты `&[u8]`.*

- [ ] **2.1. Модуль сегментации и макро-объектов (Раздел 4):**
  - [ ] Написать функцию выделения контура книги на фоне сканера.
  - [ ] Реализовать фильтр изоляции боковых артефактов («бокпушинг»).
- [ ] **2.2. Модуль геометрического анализа (Раздел 5):**
  - [ ] Реализовать алгоритм детекции четырех вершин (Кэнни -> Хаф -> Поиск пересечений).
  - [ ] Написать математическую функцию устранения деформации корешка (Диварпинг) через тригонометрическую интерполяцию пикселей.
- [ ] **2.3. Конвейер фильтрации и бинаризации (Раздел 6):**
  - [ ] Написать поканальные фильтры (Текст, Иллюстрация, Цветной 24-бит).
  - [ ] Реализовать алгоритм динамического трэшхолда (адаптивная бинаризация старой бумаги).
- [ ] **2.4. Инженерия файлов PDF (Раздел 7):**
  - [ ] Написать упаковщик очищенных страниц в 1-битный формат CCITT Group 4.
  - [ ] Реализовать сборщик PDF-файла из потока сжатых страниц.

---

## Этап 3: Слой представления и логики на Flutter / Dart
*Бизнес-логика, управление очередью и состояниями без привязки к железу.*

- [ ] **3.1. Интеграция захвата (Раздел 3):**
  - [ ] Написать Dart-интерфейс для работы с подсистемой Sane API (сканирование).
  - [ ] Реализовать логику матрицы внешних паразитных помех.
- [ ] **3.2. Архитектура BLoC (Раздел 8):**
  - [ ] Реализовать состояния матрицы BLoC:
    - [ ] `ScanInInit` (Инициализация оборудования)
    - [ ] `ScanInReady` (Готовность к работе, ожидание оператора)
    - [ ] `ScanInProgress` (Идет сканирование / Захват с Sane)
    - [ ] `ProcessingInCore` (Передача буфера в Rust-ядро через FFI)
    - [ ] `PreviewUpdate` (Обновление миниатюры на экране)
    - [ ] `SavingPage` (Упаковка в CCITT Group 4 и запись на диск)
- [ ] **3.3. Графический интерфейс (UI):**
  - [ ] Сверстать экран сканирования с интерактивной сеткой (Drag-and-Drop вершин четырехугольника).
  - [ ] Создать панель ручной корректировки параметров (ползунки порогов фильтров).

---

## Этап 4: Отказоустойчивость, Очереди и Сессии
*Защита данных от внезапных сбоев электропитания и падения системы.*

- [ ] **4.1. Локальная база данных (Раздел 9):**
  - [ ] Развернуть хранилище Isar (или SQLite) под управление очередью.
  - [ ] Описать структуру транзакционной сессии сканирования книги.
- [ ] **4.2. Механизм восстановления сессий:**
  - [ ] Написать логику сохранения сырого буфера во временный файл (`/tmp`) до подтверждения успешной обработки.
  - [ ] Реализовать горячий рестарт комплекса (автоматическое чтение UUID недописанной книги из БД при запуске).

---

## Этап 5: Тестирование и Сборка
- [ ] **5.1. Модульные тесты ядра (Rust):**
  - [ ] Написать тесты на бинаризацию и диварпинг по захардкоженным массивам пикселей.
  - [ ] Проверить отсутствие утечек памяти (`cargo valgrind` / `clippy`).
- [ ] **5.2. Сборка комплекса:**
  - [ ] Скомпилировать Rust-ядро в `.so` библиотеку.
  - [ ] Выполнить `flutter build linux --release` для создания финального дистрибутива.
